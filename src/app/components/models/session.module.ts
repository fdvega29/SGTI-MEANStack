import { usersModule } from './user.module';

export class sessionUser {
    dataUser: {
        token: string,
        expiresIn: string,
        usuario: usersModule
    };
  usuario: usersModule
}
